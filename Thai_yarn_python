1.	C:\Whizz\ui-lesson-player\ui-lesson-player\.env.development file change the first two lines with the following
	
REACT_APP_HOSTNAME_API=test-api.whizz.com
REACT_APP_HOSTNAME_CDN=localhost:8000

2. 	Goto C:\Whizz\ui-lesson-player\ui-lesson-player\node_modules\@whizz\api-model\dist\es.js 
			a.Line no : 844 in replace with this.

//return "https://".concat(process.env.HOSTNAME_CDN || process.env.REACT_APP_HOSTNAME_CDN, "/").concat(locale.country, "/").concat(locale.id);
return "http://".concat(process.env.HOSTNAME_CDN || process.env.REACT_APP_HOSTNAME_CDN);
			
3. Then run git bash in ui-lesson-player
	
	yarn
	then repeat the first step and then
	
	yarn start
	
4. Goto comopiled-th or mx - which ever the repo you are going to test 
	
	in Command prompt type python -m http.server
	
5.	Now go to local host in chrome which is already launched and replace
	http://localhost:3000/MA_GBR_0525BAp0600?locale=th-th&debug&all 
