Download and install yarn

https://classic.yarnpkg.com/en/docs/install/#windows-stable

Download and intall nodejs

https://nodejs.org/en/

install python from

https://www.python.org/downloads/

then

npm install

Run python server in compiled folder (eg: content-cmpiled-en-gb)

Edit `.env.development` file

        - REACT_APP_HOSTNAME_API=api.whizz.localhost
        + REACT_APP_HOSTNAME_API=test-api.whizz.com
        - REACT_APP_HOSTNAME_CDN=cdn.whizz.localhost
        + REACT_APP_HOSTNAME_CDN=localhost:8000
		
Edit `ui-lesson-player\node_modules\@whizz\api-model\dist\es.js` file

        - return "https://".concat(process.env.HOSTNAME_CDN || process.env.REACT_APP_HOSTNAME_CDN, "/").concat(locale.country, "/").concat(locale.id);
        + return "http://".concat(process.env.HOSTNAME_CDN || process.env.REACT_APP_HOSTNAME_CDN);		

In `ui-lesson-player` repo folder launch the server (in Git Bash):

        yarn && yarn start

